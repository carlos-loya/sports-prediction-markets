leagues:
  soccer:
    - name: "Premier League"
      fbref_id: "9"
      country: "England"
      seasons: ["2024-2025"]

  basketball:
    - name: "NBA"
      seasons: ["2024-25"]

rate_limits:
  fbref:
    requests_per_minute: 4
  nba_api:
    requests_per_minute: 15
  kalshi:
    reads_per_second: 10

edge_detection:
  min_edge_pct: 0.03
  min_volume: 10
  min_time_to_close_hours: 0.5
  kelly_fraction: 0.1

storage:
  bronze_path: "data/bronze"
  silver_path: "data/silver"
  gold_path: "data/gold"
  duckdb_path: "data/gold/sports_analytics.duckdb"
